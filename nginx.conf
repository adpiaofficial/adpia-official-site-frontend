server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # 핵심: 요청한 경로에 파일이 없으면 index.html을 보여줌
        try_files $uri $uri/ /index.html;
    }
}